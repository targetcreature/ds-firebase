{
  "rules": {
    "$room_id": {
      ".write": "auth.uid === root.child('owner').val() || root.child('owner').val() === null",
      ".read": true,
      "$uid": {
        ".write": "auth.uid === $uid"
      }
    }
  }
}